services:
  bot:
    build:
      context: .
      dockerfile: nest.Dockerfile
    volumes:
      - ./src:/app/src
      - ./auth_info_baileys:/app/auth_info_baileys
    environment:
      - NODE_ENV=development
